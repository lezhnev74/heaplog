services:
  node:
    build:
      dockerfile: Dockerfile
    user: $DOCKER_USER_ID:$DOCKER_GROUP_ID #edit in .env
    container_name: nodejs
    working_dir: /var/www
    volumes:
      - ./:/var/www
    network_mode: host